# Stack

- Vite+React+TypeScript frontend
- FastAPI+LangGraph backend
- nhost/Hasura/Postgres

# Local Development

1. npx nhost up
2. cd backend && uvicorn app.main:app --reload --port 8000
3. cd frontend && npm run dev

# Status

Section 1 of 16 complete
